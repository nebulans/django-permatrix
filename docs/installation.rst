============
Installation
============

At the command line::

    $ easy_install django-permatrix

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-permatrix
    $ pip install django-permatrix
